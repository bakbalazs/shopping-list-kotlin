<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.unideb.shoppinglist">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingList">
        <activity
            android:name="hu.unideb.shoppinglist.pages.activities.login.LoginActivity"
            android:theme="@style/Theme.ShoppingList.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--        <activity-->
<!--            android:name=".pages.HomeActivity"-->
<!--            android:theme="@style/Theme.ShoppingList.NoActionBar">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->
        <activity
            android:name=".pages.activities.home.HomeActivity"
            android:theme="@style/Theme.ShoppingList.NoActionBar" />
        <activity
            android:name="hu.unideb.shoppinglist.pages.activities.registration.RegistrationActivity"
            android:theme="@style/Theme.ShoppingList.NoActionBar" />
    </application>

</manifest>